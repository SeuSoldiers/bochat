services:
  postgres:
    image: postgres:16
    container_name: chat-postgres
    restart: unless-stopped
    network_mode: host
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-chat_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-chat_password}
      POSTGRES_DB: ${POSTGRES_DB:-chat_db}
      TZ: Asia/Shanghai
    volumes:
      - chat_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-chat_user} -d ${POSTGRES_DB:-chat_db} -h 127.0.0.1"]
      interval: 10s
      timeout: 5s
      retries: 5
    command: >
      postgres
      -c listen_addresses=127.0.0.1

  redis:
    image: redis:7
    container_name: chat-redis
    restart: unless-stopped
    network_mode: host
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-redis_password} --bind 127.0.0.1
    volumes:
      - chat_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-redis_password}", "-h", "127.0.0.1", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chat-backend
    restart: unless-stopped
    network_mode: host
    environment:
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: ${SERVER_PORT:-48888}
      DATABASE_URL: postgres://${POSTGRES_USER:-chat_user}:${POSTGRES_PASSWORD:-chat_password}@127.0.0.1:5432/${POSTGRES_DB:-chat_db}
      REDIS_URL: redis://:${REDIS_PASSWORD:-redis_password}@127.0.0.1:6379
      JWT_SECRET: ${JWT_SECRET:-your-super-secret-key-change-in-production}
      SUPER_ADMIN_PASSWORD: ${SUPER_ADMIN_PASSWORD:-Admin123456}
      FILE_STORAGE_PATH: /app/data/files
      LOG_DIR: /app/logs
      RUST_LOG: ${RUST_LOG:-info,chat_platform=debug}
    volumes:
      - chat_file_data:/app/data
      - chat_logs:/app/logs
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    build:
      context: ./ui-vue
      dockerfile: Dockerfile
    container_name: chat-frontend
    restart: unless-stopped
    network_mode: host
    depends_on:
      - backend

volumes:
  chat_postgres_data:
  chat_redis_data:
  chat_file_data:
  chat_logs:
